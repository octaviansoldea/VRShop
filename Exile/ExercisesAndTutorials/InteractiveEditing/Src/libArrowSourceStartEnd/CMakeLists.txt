cmake_minimum_required(VERSION 2.6)

PROJECT(libArrowSourceStartEnd)

SET(CMAKE_DEBUG_POSTFIX "D")

FIND_PACKAGE(VTK)
#IF(NOT VTK_DIR)
#  MESSAGE(FATAL_ERROR "Please set VTK_DIR.")
#ENDIF(NOT VTK_DIR)
#INCLUDE(${VTK_USE_FILE})


# Find VTK.
FIND_PACKAGE(VTK REQUIRED)
IF(VTK_USE_FILE)
  INCLUDE(${VTK_USE_FILE})
ENDIF(VTK_USE_FILE)



SET(SRCS ${SRCS}
    ArrowSourceStartEnd.h ArrowSourceStartEnd.cpp
	)

ADD_LIBRARY(libArrowSourceStartEnd ${SRCS})






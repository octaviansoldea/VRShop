PROJECT(libPicker)
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

SET(CMAKE_DEBUG_POSTFIX "D")

INCLUDE_DIRECTORIES(
	${OPENSCENEGRAPH_INCLUDE_DIRS}
)

SET(SRCS
	OSGPicker.h OSGPicker.cpp
)

ADD_LIBRARY(libPicker
  ${SRCS}
)

SET_PROPERTY(TARGET libPicker PROPERTY FOLDER Utils)